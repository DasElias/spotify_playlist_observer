{% if songImage is null %}
	<div class="bg-dropdown-grey flex items-center justify-center w-full h-full text-music-grey text-xl rounded-sm">
		<i class="bi bi-music-note-beamed"></i>
	</div>
{% else %}
	<img aria-hidden="false" draggable="false" loading="eager" src="{{ songImage }}" class="w-full h-full">
{% endif %}
